# Web (FastAPI that uses Pydantic v1)
fastapi==0.95.2
uvicorn[standard]==0.30.6

# HTTP (async + sync)
httpx==0.27.2
requests==2.32.3

# Env + fast JSON
python-dotenv==1.0.1
orjson==3.10.7

# Airtable client (uses Pydantic v1 models)
pyairtable==2.3.3

# Explicit Pydantic v1 (to match FastAPI 0.95.x + pyairtable)
pydantic==1.10.15
