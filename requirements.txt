# Core web framework
fastapi==0.115.0
uvicorn==0.30.6

# HTTP requests (async + sync)
httpx==0.27.2
requests==2.32.3

# Environment variable management
python-dotenv==1.0.1

# Data handling (pin to Pydantic v1 for pyairtable)
pydantic==1.10.15
typing-extensions==4.12.2

# Airtable client (uses Pydantic v1 types)
pyairtable==2.3.3

# Optional (fast JSON)
orjson==3.10.7
