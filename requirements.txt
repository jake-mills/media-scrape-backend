# Core web framework
fastapi==0.115.0
uvicorn==0.30.6

# HTTP requests (async + sync)
httpx==0.27.2
requests==2.32.3

# Environment variable management
python-dotenv==1.0.1

# Data handling
pydantic==2.8.2

# (Optional, but strongly recommended for JSON debugging/logging)
orjson==3.10.7

# Airtable SDK (modern official community library)
pyairtable
